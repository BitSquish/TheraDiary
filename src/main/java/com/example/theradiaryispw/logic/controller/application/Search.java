package com.example.theradiaryispw.logic.controller.application;

import javafx.scene.control.CheckBox;
import javafx.scene.control.TextField;

public class Search {

    public void search(TextField nomeP, TextField cognomeP, TextField cittaP, CheckBox inPresenza, CheckBox online, CheckBox pag, CheckBox includiCategorie) {
        System.out.println(nomeP.getText());
    }
}
